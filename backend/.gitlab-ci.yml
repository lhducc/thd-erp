include:
  - template: Security/SAST.gitlab-ci.yml

stages:
    - lint
    - test
    - build
    - deploy

build:
  stage: build
  script:
    - echo "build"

deploy:
  stage: deploy
  script: echo "deploy"
