export type DecisionType = {
  decision_type_id: string;
  decision_type: string;
  decision_group: string;
  description: string;
  created_date: string; // ISO date string, e.g., "2025-06-17T00:00:00Z"
};
